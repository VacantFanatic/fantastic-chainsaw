# pages/field-notes/field-notes.html and every pages/field-notes/posts/*.html
# are machine-generated by netlify/functions/publish.mjs on every publish,
# outside of a local `prettier --write` pass. Prettier's HTML printer is
# whitespace-sensitive in ways that are fragile to hand-match inside a
# template string, and a mismatch would turn CI red after every future
# publish -- a recurring nuisance, not a one-time bug. See CLAUDE.md.
pages/field-notes/field-notes.html
pages/field-notes/posts/*.html
