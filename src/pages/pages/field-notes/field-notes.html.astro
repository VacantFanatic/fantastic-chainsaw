---
// Legacy URL. The old site published this path, and a published URL is a
// promise -- so it redirects permanently rather than 404ing.
export const prerender = false;
return Astro.redirect("/field-notes", 301);
---
